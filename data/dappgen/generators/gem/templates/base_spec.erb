# frozen_string_literal: true

RSpec.describe <%= gem_name.to_s.camelize %> do
  it 'has a version number' do
    expect(<%= gem_name.to_s.camelize %>::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
